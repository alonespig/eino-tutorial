# 测试环境申请指南

## 申请流程
1. 在内部工单系统提交「测试环境申请」工单，填写项目名、所需服务、使用时长。
2. 直属主管审批通过后，运维组会在 1 个工作日内分配环境。
3. 环境默认有效期为 14 天，到期前 2 天会邮件提醒，可在工单中申请续期一次。

## 注意事项
- 测试环境禁止导入真实用户数据，必须使用脱敏数据。
- 每个项目最多同时占用 2 套测试环境。
- 测试环境数据库账号为只读 + 测试库读写，不允许访问生产库。
